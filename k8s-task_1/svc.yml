apiVersion: v1

kind: Service

metadata:
  name: myapp-service
  namespace: myapp
  labels:
    project: calculator-app

spec:
  type: NodePort
  selector:
    app: calculator
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080 
